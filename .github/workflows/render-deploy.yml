name: Render Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment:
      name: production
      url: https://collabhub-7jj1.onrender.com

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deployment Success
        run: echo "Deployment linked with GitHub environment"
